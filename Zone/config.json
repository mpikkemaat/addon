{
  "name": "Zone",
  "version": "0.1",
  "slug": "zone",
  "description": "Connects the zone border router to the mqtt broker",
  "arch": ["armhf", "amd64"],
  "startup": "before",
  "boot": "auto",
  "auto_uart": true,
  "options": {
    "serialport": "/dev/ttyUSB0"
  },
  "schema": {
    "serialport": "str"
  }
}

